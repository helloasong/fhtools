# Core dependencies
# 注意：如果目标机器是云桌面/虚拟机（CPU 不支持 AVX2），请使用 requirements-legacy-cpu.txt
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.2.0
scipy>=1.10.0

# Optimal Binning (本次新增)
optbinning>=0.21.0

# GUI Framework
PyQt6>=6.4.0
qt-material>=2.14
pyqtgraph>=0.13.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Excel I/O
openpyxl>=3.1.0

# Build tool (optional, for packaging)
pyinstaller>=6.0.0

# Development tools (optional)
jupyterlab>=4.0.0
