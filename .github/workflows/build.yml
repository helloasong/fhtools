name: Build All Platforms

# 注意：此工作流使用标准依赖（NumPy 2.x），适合现代 CPU
# 对于云桌面/虚拟机（无 AVX2），请使用 build-windows-legacy.yml

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  # ========== Windows 打包（标准版） ==========
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          pip install -r requirements.txt

      - name: Build application
        run: |
          pyinstaller FHBinningTool_windows.spec

      - name: Upload Windows build
        uses: actions/upload-artifact@v4
        with:
          name: FHBinningTool-windows
          path: dist/FHBinningTool/
          retention-days: 30

  # ========== macOS 打包（按需启用） ==========
  # build-macos:
  #   runs-on: macos-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #     - name: Set up Python 3.11
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.11'
  #     - name: Install dependencies
  #       run: |
  #         python -m pip install --upgrade pip
  #         pip install pyinstaller
  #         pip install -r requirements.txt
  #     - name: Build application
  #       run: bash build_all.sh
  #     - name: Upload macOS build
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: FHBinningTool-macos
  #         path: release/*
  #         retention-days: 30

  # ========== Linux 打包（按需启用） ==========
  # build-linux:
  #   runs-on: ubuntu-22.04
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #     - name: Install system dependencies
  #       run: |
  #         sudo apt-get update
  #         sudo apt-get install -y --no-install-recommends \
  #           libgl1-mesa-glx libglib2.0-0 libx11-6 libxext6 libxrender1
  #     - name: Set up Python 3.11
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.11'
  #     - name: Install dependencies
  #       run: |
  #         python -m pip install --upgrade pip
  #         pip install pyinstaller
  #         pip install -r requirements.txt
  #     - name: Build application
  #       run: bash build_all.sh
  #     - name: Upload Linux build
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: FHBinningTool-linux
  #         path: release/*
  #         retention-days: 30
