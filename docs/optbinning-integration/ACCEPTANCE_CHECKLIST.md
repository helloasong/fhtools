# Optbinning 集成验收清单

> 项目: FHBinningTool Optbinning 集成  
> 版本: v1.0  
> 日期: 2026-03-11

---

## 1. 准备工作

### 1.1 安装依赖

```bash
# 进入项目目录
cd /Users/songxianhe/Documents/trae_projects/fhtools

# 安装 Python 依赖
pip install pandas numpy PyQt6

# 安装 Optbinning (可选但推荐)
pip install optbinning>=0.19.0
```

### 1.2 准备测试数据

使用项目自带的 mock 数据：
```
tests/mock_data.xlsx
```

或创建简单测试数据：
```python
import pandas as pd
import numpy as np

np.random.seed(42)
n = 10000

df = pd.DataFrame({
    'age': np.random.randint(18, 70, n),
    'income': np.random.lognormal(8, 1, n),
    'gender': np.random.choice(['M', 'F'], n),
    'target': np.random.binomial(1, 0.1, n)
})

df.to_csv('test_data.csv', index=False)
```

---

## 2. 基础功能验收

### 2.1 应用启动

- [ ] 运行 `python -m src.ui.main_window`
- [ ] 应用正常启动无报错
- [ ] 窗口标题显示正确

### 2.2 默认分箱方法

- [ ] 打开"分析与调参"页面
- [ ] 分箱方法下拉框默认选中"🎯 最优分箱 (推荐)"
- [ ] 分隔线正确显示
- [ ] 其他6种算法在分隔线下方

### 2.3 数据导入

- [ ] 切换到"数据导入"页面
- [ ] 选择 CSV/Excel 文件
- [ ] 数据预览正常显示
- [ ] 右键列名可设为目标变量

---

## 3. 最优分箱功能验收

### 3.1 配置面板

- [ ] 显示"基础配置"分组框
- [ ] 求解器下拉框 (CP/MIP/LS)
- [ ] 优化目标下拉框 (IV/JS/Hellinger/Triangular)
- [ ] 单调性趋势下拉框 (Auto/Ascending/Descending/...)
- [ ] 箱数范围 (min-max)
- [ ] 求解时间限制

### 3.2 恢复推荐值

- [ ] 点击"⚡ 恢复推荐"按钮
- [ ] 弹出确认对话框
- [ ] 对话框显示当前数据规模
- [ ] 表格显示推荐参数
- [ ] 点击"应用"后参数更新

### 3.3 高级参数面板

- [ ] 显示"▼ 高级选项"按钮
- [ ] 点击展开显示8个参数
- [ ] 参数带复选框 (可设为 None)
- [ ] 再次点击折叠

### 3.4 富文本提示

- [ ] 鼠标悬停在 [?] 图标上
- [ ] 延迟 500ms 后显示提示框
- [ ] 提示框显示 HTML 格式内容
- [ ] 包含标题、说明、推荐值

---

## 4. 求解功能验收

### 4.1 运行分箱

- [ ] 选择数值型变量
- [ ] 点击"运行"按钮
- [ ] 按钮变为"运行中..."禁用状态
- [ ] 求解状态显示"🟡 求解中..."

### 4.2 结果展示

- [ ] 求解完成后状态变为"🟢 最优解"
- [ ] 显示求解时间
- [ ] 显示目标函数值 (IV)
- [ ] 分箱图显示分箱结果
- [ ] 表格显示详细指标 (WOE, IV, Bad Rate)

### 4.3 单调性指示器

- [ ] 分箱图右上角显示趋势图标
- [ ] 图标与配置的单调性一致
- [ ] 窗口大小变化时位置正确

---

## 5. 分类型变量验收

### 5.1 类型识别

- [ ] 导入包含分类变量的数据
- [ ] 变量列表显示 🏷️ 图标
- [ ] 数值型变量显示 🔢 图标

### 5.2 分类型分箱

- [ ] 选择分类变量
- [ ] 配置面板显示 dtype 选择
- [ ] 选择"分类型"
- [ ] 显示 cat_cutoff 参数
- [ ] 运行分箱正常

---

## 6. 批量处理验收

### 6.1 多选变量

- [ ] 按住 Ctrl 点击多个变量
- [ ] 按住 Shift 范围选择
- [ ] 批量分箱按钮显示选择数量

### 6.2 批量分箱

- [ ] 点击"批量分箱"按钮
- [ ] 确认对话框显示变量列表
- [ ] 点击"确定"开始批量分箱
- [ ] 进度对话框显示进度条
- [ ] 变量列表实时更新状态
- [ ] 可点击"取消"终止

### 6.3 结果查看

- [ ] 完成后显示成功/失败统计
- [ ] 点击变量可查看详细结果
- [ ] 失败变量显示原因

---

## 7. 导出功能验收

### 7.1 JSON 导出

- [ ] 切换到"导出"页面
- [ ] 选择导出目录
- [ ] 点击"导出 Optbinning 配置 (JSON)"
- [ ] JSON 文件成功生成
- [ ] 文件内容包含完整配置

### 7.2 JSON 内容验证

```bash
# 检查 JSON 格式
python -c "import json; json.load(open('optbinning_config_*.json'))"

# 验证关键字段
# - version
# - export_time
# - project_name
# - variables.{var}.splits
# - variables.{var}.bin_stats.total_iv
# - variables.{var}.config.solver
```

---

## 8. 兼容性验收

### 8.1 传统算法兼容

- [ ] 切换回"等频分箱"
- [ ] 显示传统配置面板
- [ ] 运行分箱正常
- [ ] 切换其他算法正常

### 8.2 降级处理

```bash
# 测试 optbinning 未安装的情况
pip uninstall optbinning -y

# 运行应用
python -m src.ui.main_window

# 验证
# - "最优分箱"选项隐藏
# - 其他功能正常
```

---

## 9. 性能验收

### 9.1 响应速度

| 数据规模 | 操作 | 期望时间 | 实际时间 |
|----------|------|----------|----------|
| 1万 | 单变量分箱 | < 3秒 | |
| 5万 | 单变量分箱 | < 5秒 | |
| 10万 | 单变量分箱 | < 10秒 | |
| 5万×5 | 批量分箱 | < 30秒 | |

### 9.2 内存占用

- [ ] 监控内存使用
- [ ] 无内存泄漏
- [ ] 大数据不崩溃

---

## 10. 问题记录

| 序号 | 问题描述 | 严重程度 | 状态 |
|------|----------|----------|------|
| | | | |

---

## 11. 验收结论

| 检查项 | 状态 |
|--------|------|
| 基础功能 | ⬜ 待验收 |
| 最优分箱 | ⬜ 待验收 |
| 高级功能 | ⬜ 待验收 |
| 分类型变量 | ⬜ 待验收 |
| 批量处理 | ⬜ 待验收 |
| 导出功能 | ⬜ 待验收 |
| 兼容性 | ⬜ 待验收 |
| 性能 | ⬜ 待验收 |

**总体结论**: ⬜ 待验收

---

**验收人**: _______________  
**验收日期**: _______________  
**备注**: 
